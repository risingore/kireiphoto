import { describe, it, expect } from 'vitest';

/**
 * Sample test.
 *
 * Replace with real tests for server utils, composables, business
 * logic. For component-level rendering tests, install
 * `@vue/test-utils` and `@nuxt/test-utils` separately.
 */
describe('example', () => {
  it('arithmetic still works', () => {
    expect(1 + 1).toBe(2);
  });
});
